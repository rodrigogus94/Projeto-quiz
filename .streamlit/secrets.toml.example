# Copie para .streamlit/secrets.toml (opcional).
# Se não existir, o app usa data/config.json com credenciais padrão.

# professor_username = "professor"
# professor_password_hash = "cole aqui o SHA-256 da senha"
